{% extends "base.html.twig" %}

{% block title %}Chum - Assign Manager To Project{% endblock %}

{% block body %}
    {{ include("navigation_bar/manageNav.html.twig") }}

    <div class="modify-my-account-grid">
        <h1>Assign Manager To Project</h1>
        <br>
        {{ form_start(form) }}
        <p>Project</p>
        {{ form_widget(form.projectCode) }}
        <br><br>
        <p>User</p>
        {{ form_widget(form.user) }}
        <br><br>
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}